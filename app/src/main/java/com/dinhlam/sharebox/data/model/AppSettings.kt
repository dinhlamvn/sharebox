package com.dinhlam.sharebox.data.model

class AppSettings {

    enum class Theme {
        LIGHT, DARK, AUTOMATIC
    }

    enum class NetworkCondition {
        WIFI_ONLY, WIFI_CELLULAR_DATA
    }
}